{
  "config": {
    "step": {
      "user": {
        "title": "Ensure Device Control",
        "description": "Configure retry settings for reliable device control.",
        "data": {
          "max_retries": "Maximum retry attempts (1-10)",
          "base_timeout": "Base timeout in milliseconds (500-5000)",
          "enable_notifications": "Enable failure notifications",
          "background_retry_delay": "Background retry delay in seconds (10-300)"
        }
      }
    },
    "error": {
      "invalid_retries": "Maximum retries must be between 1 and 10",
      "invalid_timeout": "Base timeout must be between 500 and 5000 milliseconds",
      "invalid_background_delay": "Background retry delay must be between 10 and 300 seconds"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Ensure Device Control Options",
        "description": "Configure retry settings for reliable device control.",
        "data": {
          "max_retries": "Maximum retry attempts (1-10)",
          "base_timeout": "Base timeout in milliseconds (500-5000)",
          "enable_notifications": "Enable failure notifications",
          "background_retry_delay": "Background retry delay in seconds (10-300)"
        }
      }
    },
    "error": {
      "invalid_retries": "Maximum retries must be between 1 and 10",
      "invalid_timeout": "Base timeout must be between 500 and 5000 milliseconds",
      "invalid_background_delay": "Background retry delay must be between 10 and 300 seconds"
    }
  }
}